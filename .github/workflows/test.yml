name: Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-26]
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

    - name: Set up Go
      uses: actions/setup-go@41dfa10bad2bb2ae585af6ee5bb4d7d973ad74ed # v5.1.0
      with:
        go-version-file: 'go.mod'

    - name: Install dependencies
      run: go mod download

    - name: Build
      run: make build

    - name: Run tests
      run: go test -v ./...

    - name: Run govulncheck
      if: matrix.os == 'ubuntu-latest'
      run: go run golang.org/x/vuln/cmd/govulncheck@latest ./...

  native-check:
    name: Native macOS Check
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

    - name: Set Xcode version
      uses: maxim-lobanov/setup-xcode@ed7a3b1fda3918c0306d1b724322adc0b8cc0a90 # v1.7.0
      with:
        xcode-version: latest

    - name: Install Native Tooling
      run: brew install swiftlint swift-format

    - name: Cache Swift Package Manager
      uses: actions/cache@d4323d4df104b026a6aa633fdb11d772146be0bf # v4.2.2
      with:
        path: native/OrbitCockpit/.build
        key: ${{ runner.os }}-spm-${{ hashFiles('native/OrbitCockpit/Package.resolved') }}
        restore-keys: |
          ${{ runner.os }}-spm-

    - name: Format Check
      run: make native/fmt

    - name: Lint Check
      run: make native/lint

    - name: Run Swift Tests
      run: make native/test
